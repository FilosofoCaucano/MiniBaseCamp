<%= form_with(model: [task.project, task, comment], data: { turbo_stream: true }) do |f| %>
    <div class="mb-2">
      <%= f.text_area :content, rows: 3, class: "w-full border p-2", placeholder: "Escribe un comentario..." %>
    </div>
    <%= f.submit "💬 Comentar", class: "btn btn-sm bg-blue-500 text-white px-3 py-1 rounded" %>
  <% end %>
  